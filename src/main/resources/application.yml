spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: database

security:
  jwt:
    token:
      secret-key: secret-key
      expired-length: 120000

oauth:
  google:
    client-id: client-id
    client-secret: client-secret
    redirect-uri: redirect-uri
    grant-type: grant-type
    post-url: post-url
    get-url: get-url

server:
  port: 80